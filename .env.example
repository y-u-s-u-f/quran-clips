# .env.example -- copy to .env and edit (install.sh does the copy for you).
#
# `.env` holds what is true of THIS MACHINE: where the binaries are, which
# ASR backend/model to load, and how to get out to the network. It is
# gitignored, which is why proxy credentials belong here and nowhere else.
#
# Every key is optional -- with ffmpeg and yt-dlp on PATH nothing needs
# configuring. Precedence: shell environment > .env > built-in default.
#
# NOTHING that changes a rendered pixel goes here. Style geometry, colour
# and type are constants in pipeline/render_text.py / render_bars.py,
# committed and reviewed -- a look-affecting value in an untracked file
# would make a render unreproducible from the repo alone.


# ---------------------------------------------------------------------------
# Binaries. Only needed when a tool is not on PATH, or to PIN an exact build.
# ---------------------------------------------------------------------------
#QC_FFMPEG=/opt/homebrew/bin/ffmpeg
#QC_FFPROBE=/opt/homebrew/bin/ffprobe
#QC_YT_DLP=/opt/homebrew/bin/yt-dlp


# ---------------------------------------------------------------------------
# Speech recognition (pipeline/transcribe.py).
# ---------------------------------------------------------------------------
# backend: mlx (Apple silicon) | faster (faster-whisper / CTranslate2, CPU or
# CUDA, portable). Default: mlx on Apple silicon, faster everywhere else.
#
# Switching backends RE-TIMES everything cut from a transcript: the two
# runtimes do not emit identical word boundaries. whisper.json records the
# backend that wrote it.
#QC_ASR_BACKEND=faster
#
# Model id OR an absolute path to a downloaded model directory -- both
# backends accept either, so an offline machine can point at local weights.
#QC_ASR_MODEL=mlx-community/whisper-large-v3-turbo
#QC_ASR_MODEL=/models/faster-whisper-large-v3-turbo-ct2
#
# Interpreter that can import the backend. Default: tools/asr-venv/bin/python
# (built by install.sh), falling back to the running python.
#QC_ASR_PYTHON=/path/to/asr-venv/bin/python


# ---------------------------------------------------------------------------
# Egress / proxy pool (pipeline/fetch.py --proxy). Needed on a cloud host,
# where YouTube bot-checks the datacentre IP outright. fetch.py and crop.py
# are the only scripts that leave the machine; everything else is local.
# ---------------------------------------------------------------------------
# Escalation is fixed: static residential -> datacentre -> fail. One sticky
# exit per fetch. Two measured constraints behind that order:
#  * A signed googlevideo URL embeds the exit IP that resolved it, so a
#    ROTATING proxy 403s the media fetch. Only sticky/static exits download.
#  * The bot check fires at the player-API stage, before any bytes move, and
#    burns PER EXIT IP. Moving to the next endpoint recovers; retrying one
#    endpoint harder makes it worse.
#
# Format: comma-separated user:pass@host:port (http:// is assumed).
#QC_PROXY_STATIC=user:pass@1.2.3.4:5001,user:pass@1.2.3.5:5002
#QC_PROXY_DATACENTER=user:pass@10.0.0.1:8001


# ---------------------------------------------------------------------------
# Framing solver (pipeline/crop.py). The second and last script that leaves
# the machine (via the `claude` CLI's own network calls, not this script's).
# ---------------------------------------------------------------------------
# crop.py shows sampled frames to a model and asks where the reciter is, then
# computes the 16:9 window from that with plain arithmetic. It runs at
# AUTHORING time and writes `crop:` and `x_offset:` into the reel's YAML.
#
# So this key does not violate the rule at the top of this file. Nothing
# reads it during a render: generate.py and the renderers never call out, and
# a reel re-renders identically from the committed config on a machine with
# no key at all. What the model decides becomes a reviewed number in a
# tracked file, which is the only form in which it may reach a pixel.
#
# It shells out to the Claude Code CLI (`claude -p`), so it needs no API key
# at all -- it rides whatever `claude` account is already logged in on this
# machine (`claude auth login`). Only needed if `claude` is not on PATH.
#QC_CLAUDE=/opt/homebrew/bin/claude


# ---------------------------------------------------------------------------
# Publishing (pipeline/publish.py). The third script that leaves the machine.
# ---------------------------------------------------------------------------
# A Meta app with a Facebook Page and the Instagram Business/Creator account
# linked to it. publish.py posts the SAME reel and caption to both.
#
# publish.py READS only FB_PAGE_ID, FB_PAGE_TOKEN and IG_BUSINESS_ACCOUNT_ID.
# The other three are kept here because they are what you need to get those
# back if a token is ever revoked. All of it comes out of one setup, once:
#   * an app at developers.facebook.com with the Instagram Graph API product
#   * a long-lived USER token with pages_manage_posts, pages_read_engagement,
#     instagram_basic and instagram_content_publish
#   * exchange it for the PAGE token (GET /me/accounts) -- that one does not
#     expire while the user token is valid, and is the only one used here
#   * IG_BUSINESS_ACCOUNT_ID = GET /<page-id>?fields=instagram_business_account
#
# These change nothing about a render, so they do not violate the rule at the
# top of this file: publish.py never touches a pixel, and every other script
# runs with none of them set.
#FB_APP_ID=
#FB_PAGE_ID=
#FB_LONG_LIVED_USER_TOKEN=
#FB_PAGE_TOKEN=
#IG_BUSINESS_ACCOUNT_ID=
#IG_USERNAME=
